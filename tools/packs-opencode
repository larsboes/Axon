#!/usr/bin/env bash
# Thin launcher: materialized OpenCode Pack deployment is implemented in
# packs-opencode.ts because safe tree hashing, atomic replacement, and JSON
# state handling are substantially clearer there than in bash.
_here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun run "$_here/packs-opencode.ts" "$@"