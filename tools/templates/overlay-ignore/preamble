# axon-__OVERLAY_NAME__ — DEFAULT-DENY whitelist. GENERATED by tools/ignore-policy.
#
# Do not edit this file. Edit the overlay's allowlist and re-render:
#   tools/ignore-policy render <overlay>   # rewrite this file
#   tools/ignore-policy check  <overlay>   # fail if it drifted
#
# This repo holds live secrets, credentials, backups and vaultwarden data.
# Rule: ignore EVERYTHING, then explicitly allow only safe, shareable scaffolding.
# Never un-ignore a file that contains a real secret value.
#
# Three sections, in this order, and the order is load-bearing: git resolves a path against
# the LAST pattern that matches it.
#   1. deny everything at the top level
#   2. the overlay's reviewed allowlist
#   3. Axon's immutable hard blocks, which therefore win over anything above

# 1) ignore all top-level entries
/*

# 2) always-safe repo files
!/.gitignore
!/README.md
