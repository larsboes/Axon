[package]
name = "markdown-root"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "A declared markdown root: the only way to get a file out of it, and the only way to write a machine-owned region back into one."
publish = false

[lib]
name = "markdown_root"
path = "src/lib.rs"

# Deliberately zero dependencies. It is also why the pattern support is two
# shapes rather than a glob crate.
[dependencies]
