[package]
name = "content-item"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The canonical Axon content-item-v2 reader contract, shared by every capability that emits one."
publish = false

[lib]
name = "content_item"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
