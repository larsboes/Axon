# See schemas/pack.toml.example for the field reference.
name         = "presentations"
description  = "Build, check, render and readiness-check slide decks as .pptx from a Python module, with talk-genre profiles (DSR/qualitative defence, empirical-CS talk), a role-based theme, and a contact-sheet verification loop. Also renders Mermaid diagrams to themed figures from the same palette, so a figure and the deck around it cannot drift apart."
skills       = ["slide-deck", "diagrams"]
license      = "MIT"
