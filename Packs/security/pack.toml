# See schemas/pack.toml.example for the field reference.
name        = "security"
description = "Guard secrets and credentials from agent visibility. Pi agent extension that blocks reading .env, .secrets, and Bitwarden items at the tool-call boundary, while allowing commands that use those secrets. Ships a Claude Code secrets-guard skill for the same doctrine."
skills      = ["secrets-guard"]
license     = "MIT"
