# Attention Control Doctrine

## The Why: ADHD and Cognitive Load
Aviation uses controlled phraseology because a distracted human under load will mishear instructions. The same applies to reading.

### Five Driving Facts
1.  **Working memory is small.** Anything not on screen is gone.
2.  **Knowing is not doing.** Work dies in the gap between understanding and execution.
3.  **Starting is the hardest step.** The first action must be obvious and small.
4.  **Time is non-linear.** "A bit of work" and "a few hours" register as the same.
5.  **Dopamine is scarce.** Visible progress (the "win") must be obvious.

## Sources

- **Shape layer:** adapted from [ayghri/i-have-adhd](https://github.com/ayghri/i-have-adhd)
  (MIT, Copyright (c) 2026 Ayoub Ghriss). The notice is retained in this pack's `LICENSE`;
  the register entry is `upstreams.toml [i-have-adhd]`.
- **Language layer:** *not in this skill.* It lives in `Packs/cognitive-load` as the sibling
  `asd-ste100` skill, which owns every
  word-, voice- and sentence-level rule and carries its own attribution and licence limits.
  Read it when the text must be parsed rather than read.

This file previously credited the language layer to "`asd-ste100` by L1nefeed". That name
matches no `asd-ste100` implementation found on GitHub — the ones that exist are `danyuchn`,
`MDsniper` and `DotDebian` — and the register held no entry for it. The claim is dropped
rather than repeated, and it is now moot: no language rule remains here to attribute.
