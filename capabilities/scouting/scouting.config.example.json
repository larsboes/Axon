{
  "_note": "Public template. Copy to axon-overlay/config/scouting.json and fill real values there — never here. Replace <knowledge-base-root> with that deployment's absolute or home-relative knowledge-store path before enabling either Obsidian source. scouting resolves config from $AXON_PERSONAL_ROOT/config/scouting.json at runtime (see src/config.rs Config::load). Every field is optional; the tool runs against empty/local dirs with zero config.",

  "_comment_interest_profile": "Directory of markdown files describing what you're interested in (one file per focus area). Convention: 'summary:' / 'current_focus:' frontmatter lines plus a '> [!quote] Charter' blockquote paragraph — score.rs parses this generically, it does not have to be an Obsidian vault. A file named exactly 'Focus.md' is treated as a hub/lens and skipped, not scored. Superseded by source entries with profiles_glob.",
  "interest_profile_dir": "",

  "_comment_events": "Directory of existing markdown event notes to cross-reference newly discovered opportunities against (annotate-only — never written to). Leave unset to skip this entirely. Superseded by source entries with events_glob.",
  "events_dir": "",

  "_comment_database": "Postgres connection string (postgresql://user:password@host:port/db). Leave unset to auto-build one from axon-overlay/config/postgres.env (capabilities/postgres, the shared local instance) — only set this to point at a different instance entirely.",
  "database_url": "",

  "_comment_embeddings": "Optional pre-computed opportunity-embeddings JSON (see README). Left as a --opp-embeddings CLI flag in practice; set here only if you always want the same file.",
  "opp_embeddings_path": "",

  "_comment_embedding_backend": "Nothing about embedding lives in this file. Interest-profile vectors come from the 'embedding' role in the overlay's inference.json (backend, model, query/document prefixes), read through libs/inference; a machine that declares no such role falls back to hash embedding. See libs/inference/README.md and src/embed.rs.",

  "_comment_sources": "Declared opportunity sources (supersedes events_dir/interest_profile_dir). Each entry declares an adapter type and location. The pipeline runs all enabled sources when no --adapter flag is given. See capabilities/scouting/README.md.",
  "sources": [
    {
      "id": "knowledge-base",
      "adapter": "obsidian-markdown",
      "path": "<knowledge-base-root>",
      "events_glob": "Atlas/Events/*.md",
      "profiles_glob": "TELOS/Focus/*.md",
      "doc": "README.md",
      "enabled": false
    },
    {
      "id": "scholarship-radar",
      "adapter": "obsidian-markdown",
      "path": "<knowledge-base-root>",
      "opportunities_glob": "Projects/Life-Plan/Applications/*.md",
      "opportunity_type": "scholarship",
      "profiles_glob": "TELOS/Personal/Scholarship Profile.md",
      "doc": "Projects/Life-Plan/Scholarship-Radar.md",
      "enabled": false
    },
    {
      "_comment": "One tracked Luma calendar. 'url' is the calendar's cal-… api id, NOT a lu.ma slug — Luma publishes no public slug→id lookup, so the adapter rejects a slug rather than building a URL that 404s. Read the id off any event: `scout --emit-json --adapter luma --location berlin` and look at raw.calendar_api_id. The id below is the public Claude Community Events calendar.",
      "id": "claude-community",
      "adapter": "luma-calendar",
      "url": "cal-TOpA5LAFfuDeFpu",
      "enabled": false
    }
  ],

  "_comment_sources_example_rss": "You can also use RSS/Atom feeds as event sources. The RSS adapter does a lightweight parse (no XML crate dependency).",
  "_comment_sources_rss_once": "An entry like this would enable RSS-based scouting:",
  "_comment_sources_rss_demo": "{\"id\": \"conference-rss\", \"adapter\": \"rss\", \"url\": \"https://example.com/feed.xml\", \"enabled\": true}",

  "_comment_calendar_base_url": "Base URL of capabilities/calendar for `scout --promote-calendar`. Loopback only — calendar binds through libs/axon-server's serve_local. Defaults to http://127.0.0.1:8087, calendar's own default port.",
  "calendar_base_url": "http://127.0.0.1:8087",

  "_comment_home_timezone": "Your home timezone, used to turn Luma's UTC instants into the naive local wall time calendar stores. DECLARE IT ONCE, in the overlay's config/deployment.env as AXON_HOME_TIMEZONE (schemas/deployment.env.example) — calendar needs the same value, and two independent copies agree until one is edited. Setting it here still overrides, but a value that disagrees with the deployment declaration is an error naming both files. NO DEFAULT either way: promoting without one would silently write the wrong hour, so --promote-calendar refuses until it resolves (or --timezone is passed). Accepts UTC, a fixed ±HH:MM offset, or one of the supported IANA zones listed in src/localtime.rs.",
  "home_timezone": "",

  "_comment_geo": "Private routing policy for physical events. Set home_latitude, home_longitude, and local_radius_km together in the overlay to classify complete coordinate pairs by great-circle distance; there are deliberately no public home or radius defaults. Sparse events fall back to the local country tokens and then timezone prefixes. allow_unknown=true is an explicit compatibility override; false leaves evidence-free events unresolved. No route drops an opportunity.",
  "geo": {
    "home_latitude": null,
    "home_longitude": null,
    "local_radius_km": null,
    "allow_countries": [],
    "allow_timezone_prefixes": [],
    "allow_unknown": false
  },

  "port": 8084
}
