# Copy this file to `<overlay>/config/postgres.env` and replace placeholders.
# Non-secret defaults are tracked for reproducibility; secrets stay private.

POSTGRES_DB=axon
POSTGRES_USER=axon
PGDATA=/var/lib/postgresql/data/pgdata
POSTGRES_PASSWORD=<required: private random password>
