# Copy this file to `<overlay>/config/home-assistant.env` and replace placeholders.
# Non-secret defaults are tracked here; secrets belong only to the overlay/private copy.

HA_URL=http://127.0.0.1:8123
HA_TOKEN=<required: long-lived HA token from secret store>
