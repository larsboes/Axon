{
  "_note": "Public template. Copy to axon-overlay/config/printing.json and fill real values there — never here. printctl resolves config from $AXON_PERSONAL_ROOT/config/printing.json at runtime (see printctl.py _cfg_path).",

  "printer_host": "PRINTER_LAN_IP",
  "moonraker_port": 7125,

  "_comment_setup": "Descriptive labels for YOUR printer. Any Klipper/Moonraker printer works; leave printer_model blank to let `doctor` discover it live.",
  "printer_model": "",
  "nozzle_mm": 0.4,
  "material": "PETG",

  "orca_bin": "/Applications/OrcaSlicer.app/Contents/MacOS/OrcaSlicer",

  "nozzle_max_c": 260,
  "bed_max_c": 90,

  "_comment_presets": "Names saved once in the OrcaSlicer GUI (see README step 2). Until set, `slice` explains what to do; control commands work without them.",
  "preset_printer": null,
  "preset_process": null,
  "preset_filament": null
}
