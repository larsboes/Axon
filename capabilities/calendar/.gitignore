# Local dev fallback config (see config.rs / README) -- never commit real values.
calendar.config.json
# Local dev fallback for the Google creds env, if ever placed here instead of the
# overlay -- never commit secrets.
calendar.env
/target

# Cargo.lock IS committed (binary crate; Axon doctrine pins everything).
