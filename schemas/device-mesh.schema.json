{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://axon.local/schemas/device-mesh.schema.json",
  "title": "Axon canonical-node connection",
  "description": "The app-private connection contract for a device joining one Axon deployment. The node may be a Mac, a home server or a hosted node; a paired device key, not the network, admits the device (PRD Q119).",
  "type": "object",
  "required": [
    "protocol_version",
    "node_id",
    "canonical_base_url"
  ],
  "additionalProperties": false,
  "properties": {
    "protocol_version": {
      "const": "axon-node/v1"
    },
    "node_id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9._:-]{0,127}$",
      "description": "Stable identity of this canonical node. It survives URL changes and migration."
    },
    "canonical_base_url": {
      "type": [
        "string",
        "null"
      ],
      "description": "The node's main address: any HTTPS origin (a tailnet name, a hosted node or an own server), checked against the platform's roots, or desktop loopback during development. Null when the device only uses the local network."
    },
    "local": {
      "type": [
        "object",
        "null"
      ],
      "description": "The node's local-network listener (libs/axon-server/src/lan.rs). Tried before canonical_base_url with a short connect timeout.",
      "required": [
        "base_url",
        "pin_sha256"
      ],
      "additionalProperties": false,
      "properties": {
        "base_url": {
          "type": "string",
          "description": "https://<name>.local[:port] or https://<private address>[:port]."
        },
        "pin_sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "SHA-256 of the node's self-signed certificate. The only certificate this device accepts at base_url."
        }
      }
    }
  }
}
